name: custom action using docker 
on:
    workflow_dispatch:
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
          - uses: mina-magdy3/github-actions/.github/actions/docker-custom-action@main
            with:
                  messsage: "hello world"
                 