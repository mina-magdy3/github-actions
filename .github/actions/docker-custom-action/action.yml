name: docker custom action
description: just say hi
inputs:
  message:
    type: string
    required: true
outputs:
  output1:
     description: 'ouput message'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.message }}
